[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ml-for-malaria"
version = "0.1.0"
description = "ML applied for Malaria"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
keywords = ["machine learning", "malaria", "haemozoin"]

authors = [
    { name = "David Kuter" }
]
maintainers = [
    { name = "David Kuter" }
]

dependencies = [
    "datamol",
    "hyperopt",
    "loguru",
    "matplotlib",
    "numpy",
    "pandas",
    "pyarrow",
    "rdkit>=2024.3.5",
    "shap",
    "scikit-learn",
    "xgboost",
]

[dependency-groups]
dev = [
    "coverage",
    "pytest",
    "ruff",
]

[tool.setuptools.packages.find]
include = ["ml_for_malaria*"]

[tool.uv]
default-groups = ["dev"]
# Repo lives on a Windows drive; WSL cannot hardlink from the Linux uv cache.
link-mode = "copy"
